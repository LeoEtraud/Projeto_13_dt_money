import styled from "styled-components";

export const TransactionsSearchForm = styled.input`
    width: 100%;
    padding: 0.5rem;
    border: 0;
    background: ${props => props.theme['gray-700']};
    color: white;
`;